# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/mobile/node_modules
/mobile/.expo
/mobile/dist
/mobile/android
/mobile/ios
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# Firebase / FCM credentials — accidentally landed in this repo via a
# browser download. The Capacitor experiment lives in a sibling repo;
# nothing in tokmato proper should ever ship Firebase config here.
google-services.json
firebase-service-account*.json

# vercel
.vercel

# legacy reference (kept locally as design source-of-truth, not in git)
/legacy

# claude code session state
/.claude

# typescript
*.tsbuildinfo
next-env.d.ts
.env.local
.env*.local
