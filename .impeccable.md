## Design Context

### Users

单一用户（self-tool）：考研备考期的成年学生，自我意识较强、技术 literacy 高、有 ADHD 倾向（聊天反复出现 "ADHD 友好" 关键词）。使用场景是日常学习/休息切换时的 token 经济记账与决策：

- **Job to be done**: 把"努力学习"和"应得的休息/消费"用诚实的会计单位连起来，让大脑在 ADHD 状态下也不焦虑、不指责自己。
- **Context of use**: 桌面优先（备考工作流），手机要能用。频繁切换 Home（启动番茄）↔ Redeem（消费）↔ Journey（看积累）。
- **Emotional goal**: confidence + calm。看到自己一年攒了 2,387 个番茄是骄傲，不是 KPI 焦虑。

### Brand Personality

**三个词**：编辑式 · 克制 · 有人味（editorial · restrained · humane）。

- **Voice/tone**: 像一份认真编排的杂志，不像生产力 app 的销售文案。短句、白话、无 emoji 噪音。中文 italic = 楷体（绝不允许浏览器伪斜体）。
- **Emotional notes**: 有趣不轻浮、高级不冷漠、ADHD 友好不说教。诚实大于优雅（spec 哲学 — `trashhour` 这种命名要保留）。

### Aesthetic Direction

**Visual tone**: editorial typographic + 中式器物隐喻。大字 serif italic 标题（英文用 Instrument Serif、中文用楷体）+ 小写 sans body（Inter Tight）+ tabular mono 数字（JetBrains Mono）+ 全大写 letter-spaced smallcaps 作为 section kicker + 1px hairline rules 分隔。

**Theme mode**: light + dark 双模式。
- Light（已实现，base）: paper `#F4EFE6` / ink `#1A1614`，暖米白底。
- Dark（待加）: 需在 design tokens 中预留 ink/paper 反转语义；实现新组件时不要 hardcode 颜色，统一走 CSS variable。

**Palette（restrained，单 accent + 少量功能色）**:
- `--tomato #E14A2B` — brand / FToken / 学习产出
- `--sage #6F8265` — HToken / 健康自控
- `--teal #4A8B8C` — 时间池（青绿，水的语义）
- `--plum #7B4A6A` — 警示（trashhour、熬夜扣费）
- `--gold #C8943A` — 数学 bonus 等少量场合
- 灰阶: ink / ink-2 / ink-3 / ink-mute / rule，不超过 5 级

**References / 调性来源**:
- ✅ 老牌报刊封面、独立杂志（NYT magazine、《读库》）— 大字 italic 标题、小 caps kicker、hairline 分隔
- ✅ 中式器物（陶罐、水缸、铜钱）— Wishlist 水池、token 隐喻
- ✅ 现代极简金融 app（Cashapp 的 typography 节奏）— 数字 mono、留白慷慨

**Anti-references（明确不要）**:
- ❌ 卡通存钱罐 / 任何"儿童 app 感"
- ❌ Marketing 噪音文案（"自评。系统不审核。长期失真..." 这类已经全砍）
- ❌ Emoji 当主图标（已替换为 SVG: 🎮→gamepad、🥤→cup）
- ❌ Gamification badge / streak 火焰 / leveling — 数据自带多巴胺，不要装饰
- ❌ 中文伪斜体（必须 `font-synthesis: none` + 楷体 unicode-range fallback）
- ❌ 焦虑/指责 tone（"你已经 X 天没学了!" → 永远不要）

### Accessibility & Inclusion

**Target**: WCAG AA。
- 正文 4.5:1，大字 3:1。当前 `--ink-3 #6B6258` on `--paper` ≈ 4.7:1 ✅。新增灰阶时保持。
- `prefers-reduced-motion: reduce` 必须支持 — 烟花 confetti / 水池波动 / 番茄填充动画都要在该 query 下退化为静态状态。
- 键盘可达：tab 导航、所有 sheet 的 Esc 关闭、focus ring 不可去掉（要重设计但不要 `outline: none` 裸奔）。
- 非颜色信号：tag 颜色用 5 种但都搭配 `#cs/#math/...` 文本 label，不依赖颜色辨识。

### Design Principles

1. **诚实大于优雅** — UI label 要功能诚实（`trashhour` / `熬夜结算` / `重要+紧急`），不要美化或营销。如果一段文字的存在只是为了"看起来更有人味"，就删掉。
   - 反 AI-tell 反例：smallcaps kicker 与 h3 title 不允许同义重述。`30 天账本` + `花到哪了` 错（两层在说同一件事）；`30 天` + `账本` 对（前者是温度/范围，后者是功能名）。
   - 中心点 `·` 仅作 inline 列表分隔符（`F · H · 时间池` / `保存 · 取消`），禁用作"概念 · 解释"装饰胶水。`账本 · 含进项与消费`、`先看这一页 · 之后从设置可再次打开` 都是反例：第二段是第一段的注释或扩写，删掉。
2. **数据自带多巴胺** — 真实的数字（"2,387 个番茄""共可花 ¥108"）就是最强的反馈。不加 gamification 装饰、不加进度奖章、不加 streak 火焰。
3. **ADHD 友好：当下专注、历史回顾、不展示罪状** — Home 只问"这个 25 分钟我要做什么?"；Journey 是回顾不是审判；从不显示"你今天还没开始"或类似 nag。
4. **杂志调、app 骨** ⭐ — editorial vocabulary（楷体大字 italic / smallcaps kicker / hairline rules / 单 accent）保留作为情感锚点，但**信息密度走 app 标准**：每一屏都让用户同时看到 balance + 主 CTA + 今日进度 + 快捷动作，而非把单个标题放大到独占整屏。
   - **大字仅用于情感峰值**：庆祝、Journey 年度回顾、settle 结算这类一年若干次的 moment
   - **日常 hero（Home / Redeem）字号上限 `text-h2`** (clamp 28-38)，绝不用 `text-display` (clamp 48-88)
   - **每屏首屏（above-the-fold）至少 3 个独立功能 chunk**（card / strip / row），不留大段 dead space
   - 新组件先问"这屏还能再装下哪块功能"，再问"这块占几行"
5. **隐喻必须映射真实经济状态** — 水池水位 = 实际剩余分钟；番茄填充 = 实际剩余时间百分比；硬币数 = 实际 token。装饰性插画（无数据映射）不允许。
6. **中文排版的尊重** — `font-style: italic` 在 CJK 上必须解析为楷体而不是伪斜体；`font-synthesis: none` 全局开启；新增字体 stack 时 `CJKKai` 永远在 Instrument Serif 之前。
7. **双主题前瞻** — 即使当前只用 light，所有颜色走 CSS variable。新组件不允许出现裸的 `#xxxxxx` 或 `rgba(...)`，要么用 token 要么先扩展 token。
